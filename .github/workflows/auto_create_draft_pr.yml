name: Auto Create Draft PR when No Existing PR for Issue Branch

on:
  push:
    branches:
      - 'issue-*'

jobs:
  create_draft_pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Fetch all branches from origin
      - name: Fetch all branches
        run: git fetch --all

      # Check if a PR already exists for this issue branch
      - name: Check existing PR
        id: check_pr
        run: |
          pr_exists=$(curl -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/${{ github.repository }}/pulls?head=${{ github.repository_owner }}:${{ github.ref }}" | jq '. | length')
          echo "PR_EXISTS=${pr_exists}" >> $GITHUB_ENV

      # Create Draft PR if none exists
      - name: Create Draft PR
        if: env.PR_EXISTS == '0'
        uses: repo-sync/pull-request@v2
        with:
          source_branch: ${{ github.ref }}
          destination_branch: 'main'
          title: 'Draft PR for ${{ github.ref }}'
          is_draft: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
